# v0.3 evidence pipeline

`crawler finding → experiment baseline → implementation evidence → verification window → Yandex Webmaster observation → observed delta (non-causal)`

This release intentionally keeps provider observations, SearchProof heuristics, and causal interpretation as separate layers.
