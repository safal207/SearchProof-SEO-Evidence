# Local provider observations

`npm run yandex:observe` writes timestamped Yandex Webmaster observation JSON files here.

Generated `*.json` files are ignored by Git because they may contain account-specific search performance data. OAuth tokens are never persisted in these reports.
